/** =========================================================================================
 * Filename: AppContainer.tsx
 *
 * Description: This component is the main container for the app.
 *
 * Contains:
 * - App container with styling
 ========================================================================================= */

import styled from "styled-components";

// Styled component for the main app container
const AppContainer = styled.div`
  height: 100vh;
  background: linear-gradient(to right, #d5d5d5, #b9c2c7);
  padding: 0;
  margin: 0;
  display: flex;
  flex-direction: column;
`;

export default AppContainer;
